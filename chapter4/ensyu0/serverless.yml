service: ensyu4-0
provider: aws

custom:
  name:
    bucket: k27c8ff627uxz-bucket-4-0

resources:
  Resources:
    Bucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: ${self:custom.name.bucket}
